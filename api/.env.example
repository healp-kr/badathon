# 공공데이터포털 인증키. 이 파일을 `.env` 로 복사하고 값을 채운다.
#
# 발급: https://www.data.go.kr — 기상청 초단기예보, 한국환경공단 통합대기환경지수,
#       기상청 생활기상지수(자외선)
#
# 포털이 인코딩/디코딩 두 버전을 주는데 엔드포인트마다 받는 쪽이 달라 둘 다 넣는다.
# 키가 없거나 조회에 실패하면 판정이 "모름"이 되고 환경 배지가 뜨지 않을 뿐,
# 추천·지도·기록은 정상 동작한다.
KMA_SERVICE_KEY_DECODED=
KMA_SERVICE_KEY_ENCODED=
